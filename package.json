{
  "name": "ghom-prettify",
  "version": "1.0.0",
  "description": "prettier extension for cast language to parsers and parse using async.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["prettier", "discord", "markdown"],
  "author": "ghom",
  "license": "ISC",
  "dependencies": {
    "prettier": "^2.1.1"
  }
}
